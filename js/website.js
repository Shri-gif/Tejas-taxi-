const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
async function loadSettings(){const {data}=await supabaseClient.from('site_settings').select('*').eq('id',1).maybeSingle();if(!data)return;document.title=(data.business_name||'Tejas Taxi')+' | Safe. Simple. On Time.';document.querySelectorAll('[data-business]').forEach(x=>x.textContent=data.business_name||'Tejas Taxi');if(data.phone)document.querySelectorAll('[data-phone]').forEach(x=>{x.textContent=data.phone;x.href='tel:'+data.phone.replace(/\s/g,'')});if(data.hero_title)document.querySelector('#heroTitle').innerHTML=esc(data.hero_title).replace(/\n/g,'<br>');if(data.hero_description)document.querySelector('#heroDescription').textContent=data.hero_description;if(data.hero_image_url)document.querySelector('#heroImage').src=data.hero_image_url;if(data.about_text)document.querySelector('#aboutText').textContent=data.about_text;if(data.whatsapp)TEJAS.whatsapp=data.whatsapp}
async function loadCars(){const {data}=await supabaseClient.from('cars').select('*').eq('active',true).order('created_at',{ascending:false});const box=document.querySelector('#fleetGrid');if(!box)return;box.innerHTML=(data||[]).map(c=>`<article class="car-card"><img src="${esc(c.image_url||'images/hero-car.svg')}" alt="${esc(c.name)}"><span class="tag">${esc(c.category||'TAXI')}</span><div class="car-body"><h3>${esc(c.name)}</h3><p>From ₹${Number(c.price_per_km||0)}/km</p><div class="meta">✓ ${esc(c.seats||'')} seats &nbsp; ✓ ${esc(c.luggage||'')} &nbsp; ✓ AC</div><div class="meta">${(c.features||[]).map(x=>'✓ '+esc(x)).join(' &nbsp; ')}</div></div></article>`).join('')}
async function loadServices(){const {data}=await supabaseClient.from('services').select('*').eq('active',true).order('created_at');const b=document.querySelector('#serviceGrid');if(!b)return;b.innerHTML=(data||[]).map((s,i)=>`<article class="service-card"><div class="icon">${esc(s.icon||'✦')}</div><small>0${i+1}</small><h3>${esc(s.title)}</h3><p>${esc(s.description||'')}</p><a href="#booking">Book this service →</a></article>`).join('')}
async function loadRoutes(){const {data}=await supabaseClient.from('routes').select('*').eq('active',true).order('created_at');const b=document.querySelector('#routesTable');if(!b)return;b.innerHTML='<div class="route header"><span>ROUTE</span><span>DISTANCE/TIME</span><span>ONE WAY</span><span>ROUND TRIP</span><span></span></div>'+(data||[]).map(r=>`<div class="route"><span>${esc(r.pickup)} → ${esc(r.destination)}</span><span>${esc(r.distance||'')}<br>${esc(r.travel_time||'')}</span><b>₹${Number(r.one_way_price||0).toLocaleString('en-IN')}</b><b>₹${Number(r.round_trip_price||0).toLocaleString('en-IN')}</b><a href="#booking">Book →</a></div>`).join('')}
async function loadReviews(){const {data}=await supabaseClient.from('reviews').select('*').eq('active',true).order('created_at',{ascending:false}).limit(6);const b=document.querySelector('#reviewGrid');if(!b)return;b.innerHTML=(data||[]).map(r=>`<article class="review-card">★★★★★<p>“${esc(r.review)}”</p><b>🟡 ${esc(r.customer_name)}</b><small>${esc(r.city||'')}</small></article>`).join('')}
document.addEventListener('DOMContentLoaded',async()=>{document.querySelector('.menu-btn')?.addEventListener('click',()=>document.querySelector('#nav').classList.toggle('open'));await loadSettings();await Promise.all([loadCars(),loadServices(),loadRoutes(),loadReviews()])});
